
language: rust
rust:
  - nightly

cache: cargo
dist: trusty
sudo: false

branches:
  only:
    - development

script:
  - ./.x-run.execute ':'':'' compile / all / debug'
  - ./.x-run.execute ':'':'' execute / bin / tester / all / debug'

